# Throws / ThrowsBy Test Engineering Report

## Student

- Student ID: 404131050
- Repository: OpenUnderstand
- Target module: `openunderstand.analysis_passes.Throws_ThrowsBy`
- Target reference kinds: `Throws` and `ThrowsBy`
- Development environment: Windows, Python 3.11.9
- CI environment: GitHub Actions, Ubuntu

## Scope

This work evaluates and strengthens the test suite for the
`Throws_ThrowsBy.py` analysis pass.

The selected analysis extracts exception references from:

- Java method declarations
- Java constructor declarations
- Java interface method declarations

## Test organization

The tests are stored under:

```text
tests/student_404131050/
├── fixtures/
│   └── java/
│       ├── malformed/
│       │   └── BrokenThrows.java
│       └── valid/
├── generated/
├── manual/
│   ├── test_environment.py
│   ├── test_throws_integration.py
│   ├── test_throws_properties.py
│   └── test_throws_throwsby.py
└── mutation-summary.md
```

Manual tests verify normal behavior, edge cases, parser-context traversal,
reference metadata, unresolved exceptions, nested scopes, constructors,
interface methods, and known defects.

Generated tests were produced using Pynguin, reviewed manually, and curated
before integration into the main test suite.

## Manual unit testing

The manual test suite covers:

- Normal method, constructor, and interface-method declarations
- Declarations without a `throws` clause
- Resolved and unresolved exception entities
- Parent-child and nested-scope relationships
- Multi-level parser-context traversal
- Reference metadata and source locations
- Malformed Java input and parser error recovery
- Direct `Throws` and inverse `ThrowsBy` reference creation
- Stable reference identity across repeated analysis passes
- Multiple-exception declarations as a documented expected failure

The malformed-input test parses an intentionally incomplete Java source file.
It verifies that ANTLR reports a syntax error while the analysis listener
continues without crashing.

The inverse-reference test verifies that a direct `Throws` relationship from
a method to an exception is accompanied by a `ThrowsBy` relationship from the
exception back to the method.

The repeated-pass test verifies that multiple persistence passes request the
same reference identities and do not change the semantic relationship.

## Property-based testing

Hypothesis was used as an optional advanced testing technique.

A property-based test generates 50 valid Java identifiers and verifies that
each exception name is preserved correctly in the generated Throws reference
metadata.

The test validates:

- Exception entity names
- Potential qualified entity names
- Scope names and long names
- Multi-digit source line and column values

The Hypothesis test completed successfully without discovering additional
failures.

Final pytest result

Collected: 32
Passed: 31
Expected failures: 1
Unexpected failures: 0
Execution time: 4.53 seconds

The expected failure documents a known defect involving declarations that
contain multiple exceptions.


Coverage result

Line coverage:     97.56%
Branch coverage:   83.33%
Combined coverage: 93.22%


Configured quality gates:

Required line coverage:   80%
Required branch coverage: 70%


Quality-gate result:
PASSED

Automated test generation

Pynguin generated seven raw test functions.

The raw output was reviewed before integration. Invalid expected-failure
tests, calls without useful assertions, and redundant behavior were removed
or refactored.

The curated generated suite contains three test functions and four pytest
test cases because one function is parameterized.

Generated tests cover:

Initial listener state
Handling a None parser context
Invalid and missing Java source locations

The generated tests provided limited additional coverage because the manual
suite already covered most executable behavior.


Mutation testing

Mutmut was executed on Ubuntu through GitHub Actions because native Mutmut
execution was not available in the Windows development environment.

Initial result:

Total mutants:     247
Killed mutants:    221
Survived mutants:   26
Mutation score:    89.47%

The surviving mutants revealed weaknesses related to:

Multi-level parent-context traversal
Callback argument propagation
Reference dictionary keys
Scope and parent-name differentiation
Multi-digit source columns
Method, constructor, and interface callback contracts

After strengthening the manual tests

Total mutants:     247
Killed mutants:    247
Survived mutants:    0
Mutation score:   100.00%

A mutation score of 100% means that all mutants generated by Mutmut were
detected by the current tests. It does not prove that the implementation is
free of all possible defects.

Discovered defect

A Java declaration may contain more than one exception:

void execute() throws IOException, SQLException {
}

The current implementation records only the final exception because the
callbacks use:

ctx.qualifiedNameList().getText().split(",")[-1]

Expected references:

execute Throws IOException
execute Throws SQLException

Actual result:

execute Throws SQLException

The defect is documented using a strict xfail regression test:

test_method_with_multiple_exceptions_records_every_reference

GitHub issue:

https://github.com/ShobeirSeifi-GH/OpenUnderstand/issues/1


Static analysis

The following checks are part of the quality workflow:

Ruff
mypy
pytest
pytest-cov
coverage quality gates
Mutmut

The final Ruff, mypy, pytest, coverage, and mutation checks completed
successfully.


Continuous integration

Two GitHub Actions workflows are used:

.github/workflows/quality.yml
.github/workflows/mutation.yml

The quality workflow performs:

Dependency installation
Ruff checks
mypy checks
pytest execution
Line and branch coverage measurement
Coverage quality-gate enforcement
Report artifact upload


The mutation workflow performs:

Baseline test execution
Mutmut analysis
Surviving-mutant reporting
Mutation artifact upload



Oracle validation

Comparison with SciTools Understand is currently pending because Understand
and a valid license are not available in the local Windows environment.

No Oracle result has been inferred or fabricated. This validation must be
completed after access to SciTools Understand becomes available.


Reproduction commands

Run the complete test suite:

python -m pytest tests\student_404131050 -v -rxX

Run tests with coverage:

python -m pytest tests\student_404131050 -v -rxX --cov=openunderstand.analysis_passes.Throws_ThrowsBy --cov-branch --cov-report=term-missing --cov-report=json:coverage-reports/coverage-final.json --cov-report=xml:coverage-reports/coverage-final.xml --cov-report=html:coverage-reports/html

Check coverage quality gates:

python scripts\check_quality_gates.py --report coverage-reports\coverage-final.json --min-line 80 --min-branch 70


Run Ruff:

ruff check tests\student_404131050 scripts

Run mypy:

python -m mypy tests\student_404131050 scripts

Final status

Manual test requirements: COMPLETE
Pytest:                  PASSED
Known defect:            XFAIL, documented in GitHub issue #1
Line coverage:           97.56%
Branch coverage:         83.33%
Mutation score:          100%
Ruff:                    PASSED
mypy:                    PASSED
Oracle validation:       Pending license